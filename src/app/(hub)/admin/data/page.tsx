import ActionFeedbackForm from "@/components/action-feedback-form";import {PageHeader} from "@/components/ui";import {requireAdmin} from "@/lib/auth";import {createServerClient} from "@/lib/supabase/server";import {bulkUpdateClubs,importClubsCsv} from "./actions";
export default async function AdminDataPage(){await requireAdmin();const db=await createServerClient();const clubs=await db.from("clubs").select("id,name,status,category,school_year").order("name").limit(500);return <div className="mx-auto max-w-6xl px-6 py-8"><PageHeader title="Club Data Tools" subtitle="CSV import and audited bulk administration for archive, category, school year, and advisor assignments."/><div className="mt-7 grid gap-6 lg:grid-cols-2"><section className="rounded-card border border-line bg-card p-6"><h2 className="text-xl font-bold">CSV import</h2><p className="mt-2 text-sm text-muted">Columns: slug, name, description, category, school_year, advisor_email, status. Existing slugs are updated. Maximum 500 rows.</p><ActionFeedbackForm action={importClubsCsv} className="mt-5 grid gap-3"><input type="file" name="csv" accept="text/csv,.csv" required/><button className="h-11 rounded-full bg-navy px-5 font-bold text-cream">Import CSV</button></ActionFeedbackForm></section><section className="rounded-card border border-line bg-card p-6"><h2 className="text-xl font-bold">Bulk actions</h2><ActionFeedbackForm action={bulkUpdateClubs} className="mt-4 grid gap-3"><div className="max-h-72 space-y-2 overflow-auto rounded-control border border-line p-3">{clubs.data?.map(c=><label key={c.id} className="flex items-center gap-2 text-sm"><input type="checkbox" name="club_id" value={c.id}/><span className="flex-1">{c.name}</span><span className="text-xs text-muted">{c.status} · {c.category??"uncategorized"} · {c.school_year??"no year"}</span></label>)}</div><select name="operation" required className="h-11 rounded-control border border-line bg-content-bg px-3"><option value="">Choose action</option><option value="archive">Archive</option><option value="category">Set category</option><option value="year">Set school year</option><option value="advisor">Assign advisor by email</option></select><input name="value" placeholder="Category, 2026-2027, or advisor email" className="h-11 rounded-control border border-line bg-content-bg px-3"/><label className="text-sm"><input type="checkbox" name="confirm" value="yes" required/> I reviewed the selected Clubs and understand this changes them together.</label><button className="h-11 rounded-full bg-navy px-5 font-bold text-cream">Apply bulk action</button></ActionFeedbackForm></section></div></div>}
