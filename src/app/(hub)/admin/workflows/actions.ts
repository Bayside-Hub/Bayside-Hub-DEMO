"use server";
import { revalidatePath } from "next/cache";
import { getCurrentUser } from "@/lib/auth";
import { createServerClient } from "@/lib/supabase/server";
import { isSupabaseConfigured } from "@/lib/supabase/config";
async function staff(){const user=await getCurrentUser();if(!user||!["staff","admin"].includes(user.role)||!isSupabaseConfigured())return null;return {user,db:await createServerClient()};}
export async function reviewWorkflow(fd:FormData){const c=await staff();if(!c)return {ok:false,message:"Staff access is required."};const kind=String(fd.get("kind")??""),id=String(fd.get("id")??""),decision=String(fd.get("decision")??""),note=String(fd.get("note")??"").trim();if(!id||!["approve","reject","waitlist","paid"].includes(decision)||note.length>1000)return {ok:false,message:"Check the decision and note."};let error=null;if(kind==="trip")error=(await c.db.from("club_trips").update({status:decision==="approve"?"approved":"rejected",reviewed_by:c.user.id,review_note:note||null}).eq("id",id).eq("status","submitted")).error;else if(kind==="event"){const row=await c.db.from("event_approval_requests").update({status:decision==="approve"?"approved":"rejected",reviewed_by:c.user.id,review_note:note||null}).eq("id",id).eq("status","pending").select("*").maybeSingle();error=row.error;if(row.data&&decision==="approve")error=(await c.db.from("events").insert({title:row.data.title,description:row.data.description,event_type:"club",club_id:row.data.club_id,start_at:row.data.start_at,end_at:row.data.end_at,location:row.data.location,price_label:"Free",published:true,created_by:c.user.id})).error;}else if(kind==="permit"){const status=decision==="approve"?"approved":"rejected";error=(await c.db.from("facility_permits").update({overall_status:status,room_status:status,security_status:status,library_status:status,av_status:status,review_note:note||null}).eq("id",id).eq("overall_status","pending")).error;}else if(kind==="reimbursement")error=(await c.db.from("club_reimbursements").update({status:decision==="approve"?"approved":decision==="paid"?"paid":"rejected",reviewed_by:c.user.id,review_note:note||null}).eq("id",id)).error;else if(kind==="registration")error=(await c.db.from("event_registrations").update({status:decision==="approve"?"approved":decision==="waitlist"?"waitlisted":"rejected",reviewed_by:c.user.id,review_note:note||null}).eq("id",id).eq("status","pending")).error;else return {ok:false,message:"Unknown workflow."};if(error)return {ok:false,message:"Decision could not be saved."};revalidatePath("/admin/workflows");revalidatePath("/events","layout");return {ok:true,message:"Decision saved."};}
